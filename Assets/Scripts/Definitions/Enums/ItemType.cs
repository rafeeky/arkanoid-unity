namespace Arkanoid.Definitions
{
    // 아이템 종류. BlockDefinition.DropItemType (nullable, null=no drop) 과
    // ItemDefinition.ItemType/EffectType 둘 다 사용.
    public enum ItemType { Expand, Magnet, Laser }
}
