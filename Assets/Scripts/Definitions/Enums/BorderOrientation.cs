namespace Arkanoid.Definitions
{
    // 'Horizontal' = 가로 BorderLength × 세로 BorderThickness (상단 테두리)
    // 'Vertical'   = 가로 BorderThickness × 세로 BorderLength (좌/우 테두리)
    public enum BorderOrientation { Horizontal, Vertical }
}
